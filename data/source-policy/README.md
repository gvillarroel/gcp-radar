# Source Policy

This directory stores offline validation reports for source URL policy checks.

`current/official-source-url-report.json` and `.md` are generated by:

```bash
node scripts/validate-official-source-urls.mjs
```

The check is report-only by default. Set `GCP_RADAR_SOURCE_POLICY_STRICT=1` to fail when non-allowlisted URLs are found.
