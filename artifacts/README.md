# Artifacts

This directory stores the validated source-of-truth documentation for the project.

Artifacts must be organized hierarchically:

- one folder per Google product
- within each product, one folder per feature
- within each feature folder, the validated documentation for that feature

This content is the canonical source used to generate final reports.

Current status:

- promoted product and feature artifacts are present
- Step 09 owns updates to this source-of-truth artifact inventory

All documentation stored here must be written in English.
